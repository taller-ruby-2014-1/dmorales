<h1>Posts</h1>

<table>
	<tr> <th>Título</th> <th>Texto</th> </tr>
	<% @posts.reverse_each do |post| %>
		<tr>
			<td><%= post.title %></td>
			<td><%= post.text %></td>
		</tr>
	<% end %>
</table>

<%= link_to "New Post", new_post_path %>